# Personal data — written directly by the app via GitHub API
# These files live in your personal fork (jaschro/logtrim) but are
# not tracked here so they don't accidentally overwrite template data.
workout-log.json
workout-log.csv
profile.json
suggested-workout.json
garmin-recent.json
z.*/
*Claude Project Instructions*

# Garmin auth tokens — NEVER commit
garmin_tokens.json

# Local-only scripts containing credentials
purge-template.bat
purge-tokens.bat
_to_delete/
